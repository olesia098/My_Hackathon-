from django.urls import path, include
from rest_framework.routers import DefaultRouter
#
# from main.views import *
#
#
# router = DefaultRouter()
# router.register('', BooksDescriptionViewSet)
#
# urlpatterns = [
#
#     path('content/', BookContentListView.as_view()),
#     path('content/<str:slug>', BookContentListViewDeleteUpdateRetriveView.as_view()),
#     path('', include(router.urls)),
# ]